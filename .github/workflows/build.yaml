name: build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  sonar-scan:
    uses: nirojasmar/base-workflows/.github/workflows/sonar-scan.yml@main
    with:
      project_key: "hobio"
      scanner_type: "dotnet"
    
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build...
        run: echo "Building images..."
